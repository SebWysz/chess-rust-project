use crate::PieceColour;

struct ArrayBoard {
    board : Vec<Vec<Option<Piece>>>,
    turn : PieceColour,
}

impl ArrayBoard {
    fn new() -> Self {
       ArrayBoard {
        board: vec![vec![Some(Piece::
    }
}